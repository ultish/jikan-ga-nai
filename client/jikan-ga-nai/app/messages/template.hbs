Messages:
<table>
  <thead>
    <tr>
      <td>
        Method #1
      </td>
      <td>
        Method #2
      </td>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        {{#each this.sortedMsgs as |msg|}}
          {{msg.id}}: {{msg.text}} by {{msg.user.username}}
          <br />
        {{/each}}
      </td>
      <td>
        hello
        {{#each this.model.edges as |m|}}
          {{m.text}}
          <br />
        {{/each}}
      </td>
    </tr>
  </tbody>
</table>

<Input id="text" @value="" />
{{outlet}}